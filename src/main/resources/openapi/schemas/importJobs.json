{
  "description": "Result set of import jobs.",
  "type": "object",
  "properties": {
    "importJobs": {
      "description": "List of import jobs",
      "type": "array",
      "items": {
        "type": "object",
        "$ref": "importJob.json"
      }
    },
    "totalRecords": {
      "description": "Total number of records in response",
      "type": "integer"
    }
  }
}